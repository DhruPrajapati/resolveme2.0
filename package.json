{
  "name": "resolveme",
  "private": true,
  "scripts": {
    "dev:client": "bun run --cwd client dev",
    "dev:server": "bun run --cwd server dev",
    "install:all": "bun install --cwd client && bun install --cwd server"
  }
}
